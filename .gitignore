# ── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# ── Ambiente virtual ─────────────────────────────────────
venv/
.venv/
env/
ENV/

# ── IDEs / editores ──────────────────────────────────────
.idea/
.vscode/

# ── Sistema operacional ──────────────────────────────────
.DS_Store
Thumbs.db

# ── Logs ─────────────────────────────────────────────────
logs/
*.log
*.log.[0-9]

# ── Artefatos de debug do OCR ────────────────────────────
debug_screenshots/

# ── Estado de runtime / persistência gerada ──────────────
# (comentários em linha própria — git NÃO aceita comentário no fim do padrão,
#  o que antes fazia estes 5 arquivos NÃO serem ignorados de fato)
# região de calibração (Pipeline A) e do painel SOLVE (Pipeline B)
calibration_region.json
solve_region.json
calibration_profiles.json
# presets do usuário
presets.json
# manutenção: prompts sem palavra e leituras OCR ambíguas
missing_prompts.json
ambiguous_ocr.json
personal_wordlists/

# ── Exports gerados ──────────────────────────────────────
missing_prompts.txt
ambiguous_ocr.txt

# ── Temporários ──────────────────────────────────────────
tmp/
*.tmp
.artifacts/
artifacts/
